# Whitespace control settings to simplify the definition of templates
whitespace_control:
  trim_blocks: true
  lstrip_blocks: true

templates:
  - template: live_check.json.j2
    filter: .
    application_mode: single
