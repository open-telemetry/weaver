templates:
  - template: "registry.md.j2"
    filter: "."
    application_mode: single
    file_name: registry.md
  # TODO - application_mode: each will be broken given the split, we may need new application modes.