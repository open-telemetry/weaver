{% for attributes_root_namespace in ctx %}
{{ ("Examples of comments for group " ~ attributes_root_namespace.root_namespace) | comment(comment_format="java") }}

    {% for attr in attributes_root_namespace.attributes %}
  {{ [attr.brief, attr.note] | comment(comment_format="java", indent=2) }}
  static {{ attr.name | screaming_snake_case }} = "";

    {% endfor %}

{% endfor %}