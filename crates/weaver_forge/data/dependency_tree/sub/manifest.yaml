name: sub
description: Sits between middle and leaf, so the graph has a depth the list does not show.
schema_url: https://example.com/dependency-tree-sub/1.0.0
dependencies:
  - name: leaf
    schema_url: https://example.com/dependency-tree-leaf/1.0.0
    registry_path: data/dependency_tree/leaf
