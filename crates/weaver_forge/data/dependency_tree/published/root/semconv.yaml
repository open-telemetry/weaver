file_format: definition/2

attributes:
  - key: published.root.key
    type: string
    stability: stable
    brief: Defined by the root registry that consumes a published middle.
    examples: ["x"]
