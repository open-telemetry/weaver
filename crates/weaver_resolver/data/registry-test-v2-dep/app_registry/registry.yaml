file_format: definition/2
metrics:
  - name: app.request.count
    requirement_level: recommended
    brief: Count of app requests.
    instrument: counter
    unit: "{request}"
    stability: stable
    attributes:
      - ref: server.address
        requirement_level: required
      - ref: server.port
        requirement_level: recommended
