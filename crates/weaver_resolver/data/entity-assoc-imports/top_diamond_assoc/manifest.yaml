name: top_diamond_assoc
description: >
  `host` is reachable by two paths, and both lead to the one definition in
  `base`. There is nothing to choose between, so the association resolves.
schema_url: https://example.com/top-diamond-assoc/1.0.0
dependencies:
  - name: middle
    schema_url: https://example.com/middle/1.0.0
    registry_path: data/entity-assoc-imports/middle_reexport
  - name: base
    schema_url: https://example.com/base/1.0.0
    registry_path: data/entity-assoc-imports/base
