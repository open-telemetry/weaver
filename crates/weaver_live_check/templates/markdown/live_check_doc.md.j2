{%- import "macros.j2" as macros -%}
<!-- Auto-generated by Weaver — do not edit -->
# Weaver Live Check Findings Reference

Weaver Live Check validates telemetry against OpenTelemetry semantic conventions and
emits structured findings as OTLP log records. This document describes the event schema,
attributes, and enumerations used by those findings.
{% for event in ctx.registry.events %}

## Event: `{{ event.name }}`

{{ macros.stability_badge(event.stability) }}

{{ event.brief }}

{% if event.note is defined and event.note | length > 0 %}
{{ event.note }}
{% endif %}
{% if event.attributes is defined and event.attributes | length > 0 %}

### Attributes

{{ macros.attribute_table(event.attributes) }}
{% endif %}
{% endfor %}

## Attribute Details
{% for attr in ctx.registry.attributes %}

### {{ macros.attr_key(attr) }}

{{ macros.stability_badge(attr.stability) }}

{{ attr.brief }}

**Type:** `{{ macros.attr_type(attr) }}`
{% if attr.note is defined and attr.note | length > 0 %}

{{ attr.note }}
{% endif %}
{% if attr.type is mapping %}

#### Values

{{ macros.enum_table(attr) }}
{% for member in attr.type.members %}
{% if member.note is defined and member.note | length > 0 %}
**`{{ member.value }}`**: {{ member.note }}
{% endif %}
{% endfor %}
{% endif %}
{% if attr.examples is defined %}

**Examples:** {{ macros.format_examples(attr) }}
{% endif %}

---
{% endfor %}
