# No `when`, so this applies to every log. Logs have no signal, so it can
# only add an attribute group.
[[live-check.matchers]]
id = "myapp.common.log"
sample_type = "log"
attribute_groups = ["myapp.common"]
