# No `when`, so this applies to every log. A log has no signal, so the
# matcher can only add an attribute group.
[[live-check.matchers]]
id = "myapp.common.log"
sample_type = "log"
attribute_groups = ["myapp.common"]
