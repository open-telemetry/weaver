# A scope has a name, so the `when` can use it directly.
[[live-check.matchers]]
id = "myapp.scope"
sample_type = "instrumentation_scope"
when = 'name.startsWith("myapp.")'
attribute_groups = ["myapp.scope"]
