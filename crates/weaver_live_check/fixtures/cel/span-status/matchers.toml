# `error.type` is conditionally required when an error occurred. A span
# records its outcome in its status, not in an attribute.
[[live-check.matchers]]
id = "myapp.checkout.failed"
sample_type = "span"
when = 'status.code == "error"'
signal = "myapp.checkout"
