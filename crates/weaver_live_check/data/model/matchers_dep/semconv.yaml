# yaml-language-server: $schema=../../../../../schemas/semconv.schema.v2.json
file_format: definition/2

attributes:
  - key: acme.tenant.id
    type: string
    brief: Identifier of the tenant the telemetry belongs to.
    stability: stable
    examples: ["t-42"]
